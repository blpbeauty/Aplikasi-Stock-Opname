version: "3.8"
services:
  stock-opname:
    build:
      context: .
      args:
        NEXT_PUBLIC_APPS_SCRIPT_URL: ${NEXT_PUBLIC_APPS_SCRIPT_URL}
    ports:
      - "3000:3000"
    restart: unless-stopped
